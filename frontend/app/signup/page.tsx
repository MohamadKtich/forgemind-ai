"use client";import Link from "next/link";import {useRouter} from "next/navigation";import {useState} from "react";import {ArrowRight,UserPlus} from "lucide-react";import {api,persistAuth} from "@/lib/api";
export default function Signup(){const router=useRouter();const [fullName,setFullName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const result:any=await api("/api/auth/register",{method:"POST",body:JSON.stringify({full_name:fullName,email,password})});persistAuth(result.access_token,result.user);router.replace("/dashboard")}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="auth-page"><section className="auth-art"><Link href="/" className="public-brand"><img src="/brand/forgemind-mark.svg" alt="ForgeMind AI"/><span>ForgeMind AI</span></Link><div className="auth-art-content"><h1>Create a controlled operations account.</h1><p>New self-registered accounts start with Viewer access. An administrator can grant factory, quality, maintenance, or administration privileges.</p><img className="hero-mini" src="/brand/factory-hero.svg" alt="Factory operations"/></div></section><section className="auth-form-wrap"><div className="auth-card"><div className="feature-icon" style={{marginBottom:18}}><UserPlus/></div><h1>Create account</h1><p>Register a local user for this ForgeMind installation.</p><form className="auth-form" onSubmit={submit}><label className="field"><span>Full name</span><input className="input" value={fullName} onChange={e=>setFullName(e.target.value)} required minLength={2}/></label><label className="field"><span>Email address</span><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="field"><span>Password</span><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/><small>At least 8 characters.</small></label>{error&&<div className="error-box">{error}</div>}<button className="btn btn-primary" disabled={busy}>{busy?"Creating account…":<>Create account <ArrowRight size={16}/></>}</button></form><div className="auth-links"><Link href="/login">Back to sign in</Link></div></div></section></main>}
