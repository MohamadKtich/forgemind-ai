"use client";import Link from "next/link";import {useRouter} from "next/navigation";import {useState} from "react";import {ArrowRight,LockKeyhole} from "lucide-react";import {api,persistAuth} from "@/lib/api";
export default function Login(){const router=useRouter();const [email,setEmail]=useState("manager@forgemind.ai");const [password,setPassword]=useState("ForgeMind#2026");const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const result:any=await api("/api/auth/login",{method:"POST",body:JSON.stringify({email,password})});persistAuth(result.access_token,result.user);router.replace(new URLSearchParams(window.location.search).get("next")||"/dashboard")}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="auth-page"><section className="auth-art"><Link href="/" className="public-brand"><img src="/brand/forgemind-mark.svg" alt="ForgeMind AI"/><span>ForgeMind AI</span></Link><div className="auth-art-content"><h1>Industrial operations, in one intelligent workspace.</h1><p>Monitor production, understand machine risk, inspect quality, create maintenance work, control digital-twin devices, and generate traceable reports.</p><img className="hero-mini" src="/brand/machine-network.svg" alt="Machine network"/></div><span className="muted" style={{fontSize:11}}>ForgeMind AI 3.0 · Local operations edition</span></section><section className="auth-form-wrap"><div className="auth-card"><div className="feature-icon" style={{marginBottom:18}}><LockKeyhole/></div><h1>Sign in to operations</h1><p>Use a local ForgeMind account. Authentication, users, roles, and audit activity are stored in the local database.</p><form className="auth-form" onSubmit={submit}><label className="field"><span>Email address</span><input className="input" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="field"><span>Password</span><input className="input" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error-box">{error}</div>}<button className="btn btn-primary" disabled={busy}>{busy?"Signing in…":<>Sign in <ArrowRight size={16}/></>}</button></form><div className="auth-note"><b>Initial local account</b><br/>manager@forgemind.ai<br/>Password: ForgeMind#2026</div><div className="auth-links"><Link href="/signup">Create local account</Link><Link href="/forgot-password">Password help</Link></div></div></section></main>}
